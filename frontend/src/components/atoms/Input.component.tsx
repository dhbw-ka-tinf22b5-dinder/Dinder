import {InputStyles} from "../../styles/input.styles";

export const Input=({ id,type})=> {
    return (
        <InputStyles
            id={id}
            type={type}
        >
        </InputStyles>
    );
}