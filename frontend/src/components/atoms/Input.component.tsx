import {InputStyles} from "../../styles/input.styles";

export const Input=({ type,name})=> {
    return (
        <InputStyles
            type={type}
            name={name}
        >
        </InputStyles>
    );
}